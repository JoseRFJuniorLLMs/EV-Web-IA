name: Deploy EV-Web

on:
  push:
    branches: [main]
  workflow_dispatch: {}

env:
  VM_IP: ${{ secrets.VM_IP }}
  VM_USER: "web2a"

jobs:
  build-and-deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build
        run: npm run build
        env:
          VITE_API_URL: https://${{ secrets.VM_IP }}/ev-api
          VITE_WS_URL: wss://${{ secrets.VM_IP }}/ev-ws
          VITE_STRIPE_PUBLIC_KEY: ${{ secrets.STRIPE_PUBLIC_KEY }}

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ env.VM_IP }} >> ~/.ssh/known_hosts 2>/dev/null
          echo "Host *
            IdentityFile ~/.ssh/deploy_key
            StrictHostKeyChecking accept-new" > ~/.ssh/config
          chmod 600 ~/.ssh/config

      - name: Deploy to VM
        run: |
          ssh ${{ env.VM_USER }}@${{ env.VM_IP }} "rm -rf /tmp/ev-web-dist && mkdir -p /tmp/ev-web-dist"
          scp -r dist/* ${{ env.VM_USER }}@${{ env.VM_IP }}:/tmp/ev-web-dist/

          ssh ${{ env.VM_USER }}@${{ env.VM_IP }} '
            set -e
            sudo rm -rf /var/www/ev-web-ia
            sudo mkdir -p /var/www/ev-web-ia
            sudo cp -r /tmp/ev-web-dist/* /var/www/ev-web-ia/
            sudo chown -R www-data:www-data /var/www/ev-web-ia
            rm -rf /tmp/ev-web-dist
            echo "EV-Web deployed to /var/www/ev-web-ia/"
          '

      - name: Health check
        run: |
          sleep 3
          HTTP_CODE=$(curl -sk -o /dev/null -w "%{http_code}" --connect-timeout 15 https://${{ env.VM_IP }}/ev-web-ia/ || echo "000")
          echo "Health check: HTTP $HTTP_CODE"
          if [ "$HTTP_CODE" = "000" ]; then
            echo "ERROR: Could not connect"
            exit 1
          fi
          echo "EV-Web OK (HTTP $HTTP_CODE)"

      - name: Notify
        if: always()
        run: echo "EV-Web deploy ${{ job.status }} - commit ${{ github.sha }}"
